<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Materias en Curso</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 p-8">
    <div class="max-w-4xl mx-auto">
        <div class="mb-8">
            <h1 class="text-3xl font-extrabold text-slate-800">Materias en Curso</h1>
            <p class="text-slate-500">Aquí tienes el detalle de tu cursada actual</p>
        </div>

        <div class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
            <table class="w-full text-left">
                <thead class="bg-slate-100/50">
                    <tr>
                        <th class="p-4 text-slate-600 font-semibold">Código</th>
                        <th class="p-4 text-slate-600 font-semibold">Nombre de la Materia</th>
                        <th class="p-4 text-slate-600 font-semibold text-center">Estado</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    {{#materiasCursando}}
                    <tr class="hover:bg-slate-50 transition-colors">
                        <td class="p-4 font-mono text-blue-600 font-medium">{{codigo_materia}}</td>
                        <td class="p-4 font-medium text-slate-800">{{nombre_materia}}</td>
                        <td class="p-4 text-center">
                            <span class="px-3 py-1 bg-emerald-100 text-emerald-700 rounded-full text-xs font-bold uppercase tracking-wide">Cursando</span>
                        </td>
                    </tr>
                    {{/materiasCursando}}
                    {{^materiasCursando}}
                    <tr>
                        <td colspan="3" class="p-12 text-center text-slate-400 italic">No estás cursando materias actualmente.</td>
                    </tr>
                    {{/materiasCursando}}
                </tbody>
            </table>
        </div>

        <div class="mt-8">
            <a href="/profile" class="inline-flex items-center text-slate-500 hover:text-blue-600 font-medium transition-colors">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
                Volver al Perfil
            </a>
        </div>
    </div>
</body>
</html>